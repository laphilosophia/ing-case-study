import {css} from 'lit';

export const addEmployeeStyles = css`
  header {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-block-end: 1.5em;
  }

  header h2 {
    margin: 0;
    color: var(--ing-primary-color);
    font-weight: 400;
  }
`;
